rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permet la lecture et l'écriture pour n'importe quel fichier
    // uniquement si l'utilisateur est authentifié.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
